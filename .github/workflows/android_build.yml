name: Android CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: set up JDK 17 (Ancient Version)
      uses: actions/setup-java@v4        # 👈 这一行只写工具名
      with:                              # 👈 参数要写在 with 下面
        java-version: '17.0.2'           # 👈 这里指定具体的旧版本
        distribution: 'zulu'             # 👈 推荐用 zulu，兼容性更好
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew MixinApk-release

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4.6.2
      with:
        name: QQPro
        path: app/dist/QQPro*.apk
