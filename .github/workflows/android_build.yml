name: Android CI

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'      # 直接用最新版 17
        distribution: 'temurin' # 换回常用的 temurin
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      # 👇 加上这段“清理门户”的代码
    - name: Remove conflicting stub files
      run: |
        echo "Removing decompiler junk..."
        # 删除 android.view 下带 $ 的奇怪文件
        find app/src/main/java/android/view -name "*\$*.java" -type f -delete
        # 删除 androidx 下带 $ 的奇怪文件
        find app/src/main/java/androidx -name "*\$*.java" -type f -delete
        echo "Done cleaning."

    - name: Build with Gradle
      # 加上 --no-daemon 省内存，加上 -x lint 跳过代码检查（Hook项目检查太多会烦死）
      run: ./gradlew MixinApk-release --no-daemon -x lint
    - name: Build with Gradle
      run: ./gradlew MixinApk-release

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4.6.2
      with:
        name: QQPro
        path: app/dist/QQPro*.apk
